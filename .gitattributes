# Normalize line endings for text files to LF (Linux-style)
* text=auto eol=lf

# Mark these file types as binary to prevent Git from modifying them
*.png binary
*.jpg binary
*.gif binary
*.pdf binary

# Explicitly mark text files you want to always be normalized and converted
# to native line endings on checkout.
*.css text
*.html text
*.js text
*.json text

# Declare files that will always have CRLF line endings on checkout.
*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.mov binary
*.mp4 binary

# Custom for Visual Studio
*.dll binary
*.exe binary

# Custom for macOS
.DS_Store binary
